# NODE + TPYESCRIPT BASIC STRATER TEMPLATE FOR PROJECTS

install nodemon as a global package for run dev server.
To start server go through the these command step by step.

1. `npm install`
2. `npm run build`
3. `npm run dev`
